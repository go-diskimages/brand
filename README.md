# go-diskimages — brand assets

Official logos for the **go-diskimages** organization.

<img src="avatar/go-diskimages.png" width="160" alt="go-diskimages">

## Formats

- **SVG** — `svg/color`, `svg/white` (white, transparent), `svg/black` (black, transparent)
- **PNG** 16→1024 px, 3 variants — `png/<variant>/<size>/`
- **JPG** (color, white background) — `jpg/<size>/`
- **ICO** (Windows) — `ico/`
- **ICNS** (macOS) — `icns/`
- **GitHub avatar** 512 px — `avatar/`
- **Social preview** 1280×640 (repo banner) — `social/`

## Per-repo logo variants (8)

Dedicated variants exist for the format codecs below. Other real repos in the
org — `diskimage` (the toolkit/CLI), `interface` (the shared contract), and
`tart-oci` — intentionally use the shared org logo above rather than a
dedicated variant. `qed`, `vdi`, `vhd`, `vhdx`, and `vmdk` are pre-provisioned
for format codecs **not yet created** in the org; the table below is an asset
inventory, not a list of existing repos.

| | repo / codec |
|---|---|
| <img src="avatar/go-diskimages-dmg.png" width="48"> | `dmg` (shipped) |
| <img src="avatar/go-diskimages-qcow2.png" width="48"> | `qcow2` (shipped) |
| <img src="avatar/go-diskimages-qed.png" width="48"> | `qed` (not yet created) |
| <img src="avatar/go-diskimages-raw.png" width="48"> | `raw` (shipped) |
| <img src="avatar/go-diskimages-vdi.png" width="48"> | `vdi` (not yet created) |
| <img src="avatar/go-diskimages-vhd.png" width="48"> | `vhd` (not yet created) |
| <img src="avatar/go-diskimages-vhdx.png" width="48"> | `vhdx` (not yet created) |
| <img src="avatar/go-diskimages-vmdk.png" width="48"> | `vmdk` (not yet created) |

---
*Auto-generated assets — rounded square badge, line glyph, color / white / black variants.*
